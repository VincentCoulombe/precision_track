name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Run Tests
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.11.13"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11.13"

      - name: Install dependencies
        run: |
          python -m ensurepip --upgrade
          python -m pip install -U pip setuptools wheel
          pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cpu
          python -m pip install -e ".[cpu, test]"

      - name: Run pytest
        run: pytest ./tests/test_ar_preprocessing.py ./tests/test_action_recognition.py ./tests/test_csv.py ./tests/test_detection_head.py ./tests/test_formatting.py ./tests/test_group_action_recognition.py ./tests/test_matching.py ./tests/test_npy.py ./tests/test_tracking.py -s -q -x
